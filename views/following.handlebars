<div class="follow-container">
    {{#if following}}
    <ul>
        {{#each following}}
        <li>
            <form action="./profile" method="get">
                <button type="submit" class="bTn2" name="otherUserId" value="{{this.id}}"><img src="./images/icons/{{this.filename}}" width="40"><span>{{this.username}}</span></button>
            </form>
            <form action="./nofollowing" method="get">
                <br>
                <button type="submit" class="bTn" name="bTn" value="{{this.id}}">Unsubscribe</button>
            </form>
        </li><br>
        {{/each}}
    </ul>
    {{else}}
        <p class="noneFollow">You've not followed any blogger~</p>
    {{/if}}
</div>